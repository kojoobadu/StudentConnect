<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="username"
        android:title="@string/pref_title_display_username"
        android:defaultValue="@string/pref_default_display_username"
        android:selectAllOnFocus="true"
        android:inputType="textEmailAddress"
        android:singleLine="true"
        android:maxLines="1" />
    <EditTextPreference
        android:key="password"
        android:title="@string/pref_title_display_password"
        android:defaultValue="@string/pref_default_display_password"
        android:selectAllOnFocus="true"
        android:inputType="textPassword"
        android:singleLine="true"
        android:maxLines="1" />

</PreferenceScreen>
